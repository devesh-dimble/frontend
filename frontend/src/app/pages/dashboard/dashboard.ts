import { Component } from '@angular/core';

/**
 * Default route: dashboard (main content is in app template; outlet stays empty here).
 */
@Component({
  selector: 'app-dashboard',
  standalone: true,
  template: '',
})
export class Dashboard {}
